FROM qwen3:14b

PARAMETER num_ctx         8192
PARAMETER num_predict     -1
PARAMETER num_keep        1024

PARAMETER temperature     0.25
PARAMETER top_p           0.9
PARAMETER top_k           40
PARAMETER repeat_penalty  1.1

# Disable thinking mode by default by injecting /nothink before every user message.
# There is no native Modelfile PARAMETER for this in Ollama 0.30.0.
# The TEMPLATE below mirrors the default qwen3 template with /nothink prepended.
TEMPLATE """
{{- if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}
{{- range .Messages }}
{{- if eq .Role "user" }}<|im_start|>user
/nothink {{ .Content }}<|im_end|>
<|im_start|>assistant
{{ else if eq .Role "assistant" }}<|im_start|>assistant
{{ .Content }}<|im_end|>
{{ end }}
{{- end }}"""

SYSTEM """
You are an expert Python engineer and data analyst.
Prefer idiomatic, efficient code.
Return complete, runnable Python with imports when asked for code.
Use tools when available to inspect files, run queries, or fetch data.
"""